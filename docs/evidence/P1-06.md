# P1-06 — Curate and encode CEAP/LIHEAP

Status: complete, with a recorded access blocker
Date: 2026-09-19

## Changed paths

- `src/shared/catalog/sources/ceap.json` — four source records (two verified, two recorded `inaccessible`).
- `src/shared/catalog/programs/ceap.json` — referral-only program entry.
- `tests/fixtures/ceap-cases.ts` — six labeled fixtures.
- `tests/unit/ceap-catalog.test.ts` — nine tests.

## Access blocker (resolution attempted, not resolved)

The Texas agency pages carrying CEAP eligibility and the state income guidelines return **HTTP 403** to this environment:

| URL | Direct request | Real browser session |
| --- | --- | --- |
| tdhca.texas.gov/comprehensive-energy-assistance-program | 403 | 403 (Akamai "Access Denied") |
| tdhca.texas.gov/income-limits | 403 | 403 |
| tdhca.texas.gov/texas-community-affairs-service-providers | 403 | 403 |

Resolution attempted per the task instruction: retried through headless Chromium with a full browser profile (same 403), then looked for official alternatives. The federal LIHEAP Clearinghouse (`liheapch.acf.hhs.gov` → `liheapch.acf.gov`) does not resolve from here. Two official alternatives were reachable and are used:

- `https://www.usa.gov/help-with-energy-bills` (HTTP 200, page last updated January 13, 2026) — LIHEAP is income based, every state sets its own requirements, apply through the state office.
- `https://www.211texas.org/` (HTTP 200) — the HHSC-run statewide referral service that routes to the local agency taking applications.

**Consequence, recorded rather than worked around:** no Texas income limit is encoded and the card offers no numeric screen. It is a referral (`rule.ceap.local_referral`, reason code `income_rules_not_verifiable`). The catalog validator reports this as an `unsupported` issue by design:

```
UNSUPPORTED ceap unverified_source: ev.ceap.no_income_table has no verified source and can only support a referral
```

## What is screened

Utility responsibility and heating/cooling need, both treated as non-blocking (`unknown` keeps the referral open), plus state. `reviewDueOn` is set to 2026-12-19 — three months, not a year — because the card should be re-attempted as soon as the state pages are readable.

## Local-provider routing (Denton and Collin counties)

No personal application was submitted anywhere. The card does **not** name a Denton or Collin county office: the fact model deliberately collects no county, and provider names could not be verified from the blocked provider directory. Routing is through 2-1-1 Texas, which serves both counties. A test asserts neither county is named in the catalog entry.

## Acceptance

- Reviewed screen and actionable official route: yes — referral screen plus 2-1-1 Texas and the federal overview, both https.
- Promises neither a particular award nor available funds: asserted by test over the value text, fallback text and summary lines; limitations state funding can run out.

## Verification

```
bunx vitest run tests/unit/ceap-catalog.test.ts    # 9 passed
EVALUATION_DATE=2026-09-19 bun run data:validate   # 3 programs, 12 sources, 1 intentional unsupported issue
```
