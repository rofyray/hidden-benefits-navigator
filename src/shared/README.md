# `src/shared`

Runtime contracts, normalization, screening and ranking shared by the browser and the
server, plus the versioned program catalog. Everything here must be free of browser-only
and server-only imports so both dependency graphs can include it.

Populated by P1-03 (contracts and catalog validation) onward. Empty at P1-01 by design.
